import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class DiagramEngine {

    // constructor(public nodesFactory: AbstractNodeFactory) {

    // }

    // createNode(id?: string): T {
    // return new NodeModel()
    // TODO: access nodes factory registry and create new node from component factory provided
    // }
}
